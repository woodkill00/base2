{
  "name": "base2-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Base2 E2E tests (Playwright)",
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  },
  "scripts": {
    "test": "playwright test",
    "test:ci": "playwright test --reporter=line",
    "install:browsers": "playwright install --with-deps chromium"
  }
}
