name: CI (repo guards)

on:
  pull_request:
  push:

jobs:
  repo-guards:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Repo guard checks
        run: |
          chmod +x scripts/repo_guard.sh
          bash scripts/repo_guard.sh
