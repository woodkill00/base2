[run]
# Coverage.py 7.15.4 crashed this Python 3.12 WSL runtime with both available
# trace implementations.  Version 7.16.0 corrects the native-tracer behavior;
# pin it explicitly so the required full gate cannot change cores implicitly.
core = ctrace
